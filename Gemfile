# frozen_string_literal: true

source "https://rubygems.org"

gem "jekyll"
# gem "tzinfo"          # Timezone 정보 관련 gem
gem 'tzinfo-data', platforms: %i[mingw mswin x64_mingw jruby]
gem "csv"             # CSV 관련 gem
gem "base64"          # base64 관련 gem

gemspec

group :test do
  gem "html-proofer", "~> 5.0"
end
